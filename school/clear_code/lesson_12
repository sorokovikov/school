// C++
constexpr auto TABLE_RACK = "dci_rack";
// Констата имени таблицы стоек. Оно неизменно и нет необходимости считывать из файла

// Golang
databaseType, err = ReadDatabaseType()
// Функция считывает из файла тип БД (mysql/postresql). БД может изменятся в зависимости от типа продукта. Разумнее было ее указать в конфиге

// Python
args = parse_args()
redis_host = args.redis_host
// Адрес хоста Redis'а получается из входных аргументов программы. Он может быть динамическим, поэтмоу задается при запуске программы
